# sudoku-solver

This sudoku solver takes advantage of binary logic to solve sudoku puzzles.
The rationale of the solution is based on cell prospects and range prospects,
and if these to does not yield a full solution, a recursive approach is taken
which returns the first possible solution (if any).

This is mainly for educational purposes.

The implementations are in different programming languages, and might be
expanded in the future.
